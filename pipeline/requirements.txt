# Document extraction pipeline (Prompt 1)
# Install: python pipeline/setup_env.py

# LangChain + LangGraph + LangSmith (same venv as extraction)
langchain>=1.0
langchain-core>=1.0
langgraph>=1.0
langsmith>=0.3
langchain-community>=0.3.27
langchain-ollama>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.1.0
anthropic>=0.40.0
# LangFlow is isolated: pipeline/.venv-langflow  (pip install langflow)

# PDF processing
pypdf>=5.4.0
pymupdf>=1.25.0

# Schema + config
pydantic>=2.10.0
python-dotenv>=1.0.1
pyyaml>=6.0.2

# HTTP fallbacks / review server (stdlib is enough; requests is convenient)
requests>=2.32.0
